#include "config.h"
#include <iostream>

void Config::load(const std::string& configFile) {
    // Placeholder code for loading configuration
    std::cout << "Loading configuration from " << configFile << std::endl;
}
